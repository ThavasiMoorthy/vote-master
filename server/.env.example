# Copy to .env and fill values for real email sending
# Example:
# SENDGRID_API_KEY=SG.xxxxx
# FROM_EMAIL=you@yourdomain.com
# ADMIN_EMAIL=mthavasi085@gmail.com
# PORT=3001

# Or use SMTP (free option via Gmail app password or other SMTP provider):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# If SENDGRID_API_KEY is empty the server will run in dev mode and print OTPs to console and return them in the response.
