# Copy this file to `.env` and replace with your Google Maps API key
# For Vite, environment variables exposed to the client must start with VITE_
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_js_api_key_here
